{
  "name": "@dodoinblue/lib-apikey",
  "version": "0.0.1",
  "description": "Basic API key mechanism with key rotation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "license": "UNLICENSED",
  "scripts": {
    "clean": "rimraf dist coverage",
    "build": "npm run clean && tsc",
    "pretty": "prettier --config .prettierrc '(lib|test)/**/*.ts' --write",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "test": "jest --env=node --colors --coverage test"
  },
  "devDependencies": {
    "@types/node": "^17.0.27",
    "eslint": "^8.14.0",
    "jest": "^27.5.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.1.4"
  }
}
